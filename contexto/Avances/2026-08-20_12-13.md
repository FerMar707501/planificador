# Avances de sesión

**Fecha y hora de cierre:** 2026-08-20 12:13 (UTC-06:00)  
**Rama:** `main`  
**Repositorio remoto:** `origin` → `https://github.com/FerMar707501/planificador.git`

## Alcance trabajado

Se trabajó únicamente en la base del backend del Planificador de Actividades. El backend será un API REST para autenticación, gestión de usuarios, auditoría y futuros respaldos cifrados. No se implementaron ubicaciones, actividades, clima ni datos SQLite de la aplicación móvil, porque esos elementos están fuera del alcance del servidor según la especificación.

## Estructura inicial creada

Se creó la estructura indicada en `especificacion_backend_nodejs.md`:

```text
src/
├── config/
│   ├── database.js
│   └── env.js
├── models/
│   ├── index.js
│   ├── usuario.model.js
│   ├── logSistema.model.js
│   └── respaldoSincronizacion.model.js
├── controllers/
│   ├── auth.controller.js
│   ├── usuario.controller.js
│   └── sincronizacion.controller.js
├── routes/
│   ├── auth.routes.js
│   ├── usuario.routes.js
│   ├── sincronizacion.routes.js
│   └── index.js
├── middlewares/
│   ├── auth.middleware.js
│   ├── admin.middleware.js
│   └── errorHandler.middleware.js
├── services/
│   ├── email.service.js
│   ├── auth.service.js
│   └── logs.service.js
├── utils/
│   ├── generarContrasenaTemporal.js
│   └── jwt.js
└── app.js
```

También se crearon `server.js`, `package.json`, `package-lock.json`, `.env.example`, `.gitignore` y `README.md`. Los archivos fuera de `models` permanecen como estructura reservada; todavía no contienen configuración de servidor, rutas, controladores ni lógica de negocio.

## Configuración inicial

- Se preparó `.env.example` con las variables `PORT`, `NODE_ENV`, `DATABASE_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN` y la configuración SMTP requerida por Nodemailer.
- Se agregó `.gitignore` para excluir `.env`, que contendrá secretos, y `node_modules`, que se instala localmente y no debe versionarse.
- Se instalaron las dependencias `sequelize` para el ORM y `pg` como controlador PostgreSQL.

## Modelos Sequelize implementados

Los modelos se implementaron a partir de `esquema_basedatos_backend.sql`. Todos usan atributos JavaScript en `camelCase` y columnas PostgreSQL en `snake_case`.

### Usuario

El modelo `Usuario` representa la tabla `usuarios` y contiene:

- `idUsuario`: UUID, clave primaria y valor generado automáticamente.
- `nombreUsuario`: texto de hasta 50 caracteres, obligatorio y único.
- `contrasenaHash`: texto de hasta 255 caracteres, obligatorio.
- `correo`: texto de hasta 150 caracteres, obligatorio, único y validado como correo electrónico.
- `nombreCompleto`: texto de hasta 150 caracteres y obligatorio.
- `telefono`: texto opcional de hasta 30 caracteres.
- `rol`: `ENUM` con valores `usuario` y `admin`; el valor inicial es `usuario`.
- `esContrasenaTemporal`: booleano inicializado en `false`.
- `activo`: booleano inicializado en `true`.
- `ultimoLogin`: fecha opcional.
- Marcas de tiempo automáticas `created_at` y `updated_at`.

### LogSistema

El modelo `LogSistema` representa la tabla `logs_sistema` y contiene:

- `idLog`: UUID y clave primaria.
- `idUsuario`: UUID opcional relacionado con `usuarios.id_usuario`; al eliminar el usuario se conserva el log y la referencia pasa a `NULL`.
- `tipoEvento`: `ENUM` limitado a `login_exitoso`, `login_fallido`, `cambio_contrasena`, `recuperacion_contrasena` y `sincronizacion`.
- `fechaEvento`: fecha obligatoria con valor inicial actual.
- `detalle`: texto opcional.

Este modelo no usa marcas de tiempo automáticas porque la tabla solo registra eventos y su fecha se almacena en `fecha_evento`.

### RespaldoSincronizacion

El modelo `RespaldoSincronizacion` representa la tabla `respaldos_sincronizacion` y contiene:

- `idRespaldo`: UUID y clave primaria.
- `idUsuario`: UUID obligatorio relacionado con `usuarios.id_usuario`; al eliminar el usuario se eliminan sus respaldos.
- `fechaSincronizacion`: fecha obligatoria con valor inicial actual.
- `tipo`: `ENUM` limitado a `semanal`, `mensual` y `manual`.
- `contenidoEncriptado`: datos binarios obligatorios mediante `BLOB`.
- `iv` y `salt`: textos obligatorios requeridos para la restauración del contenido cifrado.
- `metadataResumen`: `JSONB` opcional para datos no sensibles, como conteos.
- `hashVerificacion`: texto obligatorio para comprobar integridad.

El modelo solo prepara la estructura. Los endpoints de respaldo seguirán siendo `stub` con respuesta `501` hasta que se defina el flujo de cifrado del cliente.

## Asociaciones configuradas

- Un `Usuario` tiene muchos `LogSistema`.
- Un `LogSistema` pertenece a un `Usuario`.
- Un `Usuario` tiene muchos `RespaldoSincronizacion`.
- Un `RespaldoSincronizacion` pertenece a un `Usuario`.

## Estado de PostgreSQL local

Se indicó que se descargó la imagen PostgreSQL mediante Docker y se creó un contenedor con credenciales. Estas credenciales aún no se han incorporado al proyecto ni se ha creado `DATABASE_URL`; por ello no se ha conectado Sequelize, no se ha aplicado el esquema y no se han creado tablas en el contenedor desde el backend.

## Control de versiones y GitHub

Se creó el commit inicial:

```text
22e639f Initialize backend structure and models
```

El commit fue publicado correctamente en `origin/main`. Al cierre de la sincronización, la rama local `main` quedó siguiendo a `origin/main`.

## Contexto persistente

Se creó la carpeta `contexto/` para conservar información del proyecto:

- `contexto/especificacion_backend_nodejs.md`: copia de la especificación del backend.
- `contexto/esquema_basedatos_backend.sql`: copia del esquema PostgreSQL.
- `contexto/Avances/`: directorio destinado a un archivo Markdown de cierre por cada sesión.

## Pendientes inmediatos

1. Configurar `src/config/env.js` para validar las variables de entorno requeridas.
2. Configurar `src/config/database.js` para conectar Sequelize al contenedor PostgreSQL mediante `DATABASE_URL`.
3. Definir y aplicar migraciones que reproduzcan exactamente la extensión, `ENUM`, tablas, índices y trigger establecidos en `esquema_basedatos_backend.sql`.
4. Inicializar los modelos desde la configuración de base de datos y comprobar la conexión local.
5. Implementar la configuración Express, middlewares globales y rutas base.

## Actualización de avance

**Fecha y hora:** 2026-08-20 16:00 (UTC-06:00)

### Conexión local con PostgreSQL en Docker

- Se configuró Sequelize para conectarse al contenedor local de PostgreSQL mediante `DATABASE_URL`.
- Se agregó `DATABASE_SSL` para permitir conexiones locales sin SSL y mantener la configuración necesaria para Neon con SSL.
- Se creó un archivo `.env` local, ignorado por Git, con la configuración de base de datos y un secreto JWT generado localmente.
- Se aplicó el esquema fuente contra la base `planificador_db` del contenedor Docker.
- Se crearon y comprobaron las tablas `usuarios`, `logs_sistema` y `respaldos_sincronizacion`.
- Se verificaron los tipos `rol_usuario`, `tipo_evento_log` y `tipo_respaldo`, los índices definidos y el trigger `trigger_usuarios_updated_at`.
- Se añadió `npm run db:verify`, que autentica la conexión y confirma que los tres modelos Sequelize pueden consultar la estructura existente sin modificar el esquema.

### Infraestructura del API

- Se agregaron Express, Helmet, CORS, bcrypt, jsonwebtoken, express-validator y Nodemailer.
- Se configuró `src/app.js` con JSON, headers de seguridad, CORS, rutas bajo `/api` y manejo centralizado de errores.
- Se configuró `server.js` para iniciar el servidor después de verificar la conexión a la base de datos, con tres intentos de conexión.
- Se añadieron los scripts `start` y `dev` en `package.json`.

### Autenticación

Se implementaron las rutas bajo `/api/auth`:

- `POST /api/auth/register`: crea cuentas con nombre de usuario, correo, contraseña, nombre completo y teléfono opcional. Las contraseñas se almacenan únicamente como hash bcrypt.
- `POST /api/auth/login`: acepta nombre de usuario o correo, comprueba la contraseña con bcrypt, genera un JWT, actualiza `ultimo_login` y registra eventos de inicio de sesión exitoso o fallido.
- `POST /api/auth/forgot-password`: genera una contraseña temporal aleatoria, guarda solo su hash, activa `es_contrasena_temporal`, registra el evento y envía la contraseña por correo. Requiere las credenciales SMTP en `.env`.
- `POST /api/auth/change-password`: exige JWT, valida la doble confirmación y una longitud mínima de ocho caracteres, actualiza el hash y desactiva el indicador de contraseña temporal.

También se agregaron:

- Generación y verificación de JWT con el payload `idUsuario` y `rol`.
- Middleware que exige el encabezado `Authorization: Bearer <token>` en rutas protegidas.
- Servicio de auditoría para registrar los eventos permitidos en `logs_sistema`.
- Servicio de correo para envío de contraseñas temporales.
- Validación de los cuerpos de solicitud con `express-validator`.
- Respuestas que no exponen `contrasenaHash`.

### Perfil del usuario autenticado

Se implementaron las rutas bajo `/api/users`:

- `GET /api/users/me`: obtiene el perfil del usuario identificado por el JWT, con `idUsuario`, nombre de usuario, correo, nombre completo, teléfono y rol.
- `PUT /api/users/me`: permite modificar únicamente correo, nombre completo y teléfono.

La actualización rechaza explícitamente campos no permitidos, incluyendo nombre de usuario y contraseña. También valida formato y longitud de datos, y controla la duplicidad de correo.

### Colección Postman

- Se creó `postman/planificador-auth.postman_collection.json`.
- La colección incluye registro, inicio de sesión, cambio de contraseña, recuperación de contraseña, consulta de perfil y actualización de perfil.
- El registro genera un nombre de usuario y correo únicos en cada ejecución.
- El inicio de sesión guarda el JWT recibido en la variable de colección `token`, usada por las solicitudes protegidas.

### Validaciones realizadas

- Se comprobó la conexión de Sequelize con PostgreSQL Docker.
- Se validaron los flujos de registro, login por nombre de usuario, login por correo y cambio de contraseña.
- Se validaron consulta y actualización de perfil, incluida la restricción que impide modificar `nombreUsuario`.
- Se validó la sintaxis de los archivos JavaScript y el formato JSON de la colección Postman.

## Actualización de despliegue

**Fecha y hora:** 2026-08-20 21:20 (UTC-06:00)

### Preparación para producción

- Se instaló Fly CLI en el equipo local para administrar el despliegue.
- Se creó `Dockerfile` con una imagen base `node:22-bookworm-slim`.
- El contenedor instala únicamente dependencias de producción mediante `npm ci --omit=dev`, expone el puerto `3000` e inicia el API con `npm start`.
- Se creó `.dockerignore` para excluir `node_modules`, `.env`, `.git`, `contexto` y `postman` de la imagen.
- Se creó `fly.toml` para la aplicación `planificador-backend-fermar707501` en la región `ord`.
- La configuración Fly usa HTTPS forzado, puerto interno `3000`, inicio automático y detención de máquinas inactivas.
- Se actualizó el servidor para escuchar explícitamente en `0.0.0.0`, requisito necesario para recibir tráfico desde Fly.

### Neon PostgreSQL

- Se conectó el proyecto a la base de datos PostgreSQL alojada en Neon mediante una variable secreta `DATABASE_URL`.
- Se aplicó el esquema de `contexto/esquema_basedatos_backend.sql` a Neon.
- Se crearon la extensión `pgcrypto`, los tipos enumerados, las tablas `usuarios`, `logs_sistema` y `respaldos_sincronizacion`, los índices y el trigger de actualización de usuarios.
- Se verificó que Sequelize puede autenticar y consultar los modelos contra Neon con SSL habilitado.
- La cadena de conexión y demás secretos no se guardaron en archivos versionados.

### Secretos de Fly

Se cargaron como secretos de la aplicación Fly:

- `DATABASE_URL`
- `DATABASE_SSL`
- `JWT_SECRET`
- `JWT_EXPIRES_IN`

Las credenciales SMTP no se configuraron en Fly. Por ello, los endpoints de registro, login, cambio de contraseña y perfil están disponibles, pero `POST /api/auth/forgot-password` requiere añadir las variables SMTP antes de utilizarse en producción.

### Despliegue de Fly.io

- Se creó la aplicación `planificador-backend-fermar707501`.
- Se construyó y publicó la imagen de producción en el registro de Fly.
- Se desplegaron dos máquinas de aplicación en la región `ord`.
- La URL pública del backend es:

```text
https://planificador-backend-fermar707501.fly.dev/
```

- Se confirmó la respuesta pública de `POST /api/auth/login` con validación de entrada.

### Versión y mensajes de inicio

- Se actualizó la versión del proyecto de `1.0.0` a `0.1.0`.
- Se normalizó `NODE_ENV` para aceptar solo `development`, `test` y `production`.
- El valor de producción se definió en `fly.toml` mediante `NODE_ENV = "production"`.
- El inicio del servidor ahora valida la conexión de base de datos y la disponibilidad de los modelos antes de abrir el puerto HTTP.

Los logs de inicio muestran:

```text
🚀 Servidor encendiendo...
📦 Versión: 0.1.0
⚙️ Modo: Producción
🔌 Conectando a la base de datos...
✅ Conectado a la base de datos.
✅ Modelos sincronizados.
🌐 Servidor disponible en el puerto 3000.
```
